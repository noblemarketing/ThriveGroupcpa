# Optional: Google Maps Embed API key for the contact page map iframe.
# Create at https://console.cloud.google.com/ → APIs & Services → Maps Embed API
# Without this key, the site uses a basic Google Maps embed URL (no billing setup required).
NEXT_PUBLIC_GOOGLE_MAPS_EMBED_API_KEY=
